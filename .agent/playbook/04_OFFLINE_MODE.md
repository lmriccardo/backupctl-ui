# Playbook: Offline mode

- Detect online status at startup + periodically
- When offline:
  - disable remote connectivity actions
  - allow local actions (list registry, inspect, view logs) where possible
- Always show "Offline" indicator + what is disabled
